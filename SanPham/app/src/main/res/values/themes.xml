<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Theme tổng thể của ứng dụng -->
    <style name="Base.Theme.SanPham" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Đây chỉ nên chứa các thuộc tính như màu chủ đạo, font toàn app, v.v -->
        <!-- Ví dụ -->
        <item name="colorPrimary">#00796B</item>
        <item name="colorOnPrimary">#FFFFFF</item>
        <item name="android:windowBackground">@color/orange</item>
    </style>

    <style name="Theme.SanPham" parent="Base.Theme.SanPham" />
</resources>
